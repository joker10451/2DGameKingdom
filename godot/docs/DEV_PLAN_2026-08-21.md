# План развития «Хроники Олдерии» (Godot 4.7.1, E:\kiro\game)

_Составлен: 21.08.2026. Актуальное состояние подтверждено headless-валидацией
(EXIT=0, 0 SCRIPT ERROR), standalone-парсом 16 фасадов и ЖИВОЙ проверкой через MCP._

---

## 1. СТАТУС НА СЕЙЧАС

| Метрика | Значение |
|---|---|
| GameWorld2D.gd | **9099 строк** (было ~13157 до рефакторинга, −31%) |
| Готовых фасадов | **16** шт. в `src/ui/modals/` |
| MCP-бридж | настроен, сервер в репозитории, слушает `127.0.0.1:55139` |
| Headless-валидация | ✅ 0 ошибок |
| Живая проверка (MCP, GUI) | ✅ пройдена 21.08.2026 (ping/screenshot/get_ui_elements/input) |

### Вынесено в фасады (16)
Inventory, Skills, Trade, Smithing, Contracts, Party, Estate, Construction,
Chest, Origin, CitizenShop, Alchemy, Stable, Shipyard, Dog, Bard.

### Осталось inline в монолите (3 крупных)
- `_build_dialogue_modal` (4420) — **НЕ трогать** (общие `dialogue_*` globals)
- `_build_event_modal` (4801) — **НЕ трогать** (размер ~1870 строк, связан с квестами)
- `_build_settlement_modal` (6744) — **НЕ трогать** (связан с Party/Estate globals)

---

## 2. ВЫЯВЛЕННЫЕ ПРОБЛЕМЫ (решено / актуально)

1. **Мусор в `.mcp/scripts/`** — ✅ решено (`.gitignore` игнорирует `*.policy.json`,
   `17*-*.gd`; удалено ~475 временных файлов; остался только `godot-mcp-server.mjs`).
2. **Скриншот в headless не работает** — ✅ решено запуском Godot с окном
   (GUI-режим доказанно снимает viewport через MCP).
3. **Бинарные ассеты не закоммичены** — актуально (см. Этап D).
4. **`run_script` в MCP НЕ реализован** в бридже — только `ping`, `screenshot`,
   `get_ui_elements`, `input`, `shutdown`. Живую проверку модалок без хоткея
   делать через `input` (key/ui), не через `run_script`.

---

## 3. ПЛАН РАБОТ (приоритеты)

### Этап A. Уборка инфраструктуры — ✅ ВЫПОЛНЕН (f5e09f4)
- A1. `.gitignore` маска для временных файлов агента ✅
- A2. Удаление висящих temp-файлов ✅
- A3. `MONOLITH_REFACTOR_PLAN.md` обновлён (16/16 фасадов) ✅

### Этап B. Живая верификация через MCP — ✅ ВЫПОЛНЕН (21.08.2026)
- B1. Запуск Godot с окном → бридж на 55139 ✅
- B2. `screenshot` снял кадр viewport (1280x720) ✅
- B3. `get_ui_elements` + `input` → проверили, что InventoryModal (фасад)
       реально открывается по клавише [I] и рендерится (скриншот подтверждён) ✅
- B4. Логи чистые (McpBridge: Listening on TCP port 55139; 0 runtime-ошибок) ✅

### Этап C. Стабилизация — не требуется
Регрессий не выявлено. Если позже обнаружатся — фикс колбэков + повторная
headless-валидация.

### Этап D. Asset pipeline (отдельная ветка, не рефакторинг) — ⏳ АКТУАЛЬНО
- D1. Судьба бинарных PNG: коммитить в LFS или держать локально
       (сейчас `.gitignore` не исключает ассеты → мусор в diff).
- D2. Проверить использование новых спрайтов (`alchemy_lab.png`, `windmill_*`,
       `animal_*`, `skeleton_*`, и т.д.) в `WorldMap2D`/`NPCGenerator`.

### Этап E. Документация MCP — ✅ ВЫПОЛНЕН (2219f7e)
- E1. `docs/MCP_USAGE.md` создан ✅
- E2. Паттерн фасада зафиксирован в `MONOLITH_REFACTOR_PLAN.md` ✅

---

## 4. РИСКИ

| Риск | Вероятность | Статус |
|---|---|---|
| Регрессия Dialogue/Event/Settlement | Высокая | Отложено до стабилизации |
| MCP-скриншот без GUI | Средняя | ✅ Решено (GUI-режим) |
| Бинарные ассеты раздуют репо | Высокая | Этап D (LFS/ignore) |
| Temp-файлы агента в коммите | Средняя | ✅ Решено (A1) |

---

## 5. БЛИЖАЙШИЙ СЛЕДУЮЩИЙ ШАГ

**Этап D** — бинарные ассеты: решить LFS vs локальный ignore, проверить
использование новых спрайтов. Вне scope рефакторинга монолита.
